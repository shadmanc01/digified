import Link from 'next/link';import {cameraStats} from '@/lib/data';export default function Cameras(){return <main className="shell"><div className="pageHead"><div className="eyebrow">Gear directory</div><h1>Cameras</h1><p className="muted">See what photographers are actually making with the camera you own—or the one you're considering.</p></div><div className="grid3">{cameraStats.map(c=><Link className="cameraCard" href={`/cameras/${c.slug}`} key={c.slug}><div><div className="eyebrow">{c.maker}</div><h3>{c.name}</h3></div><div className="cameraMeta"><span>{c.photos} public photos</span><span>{c.users} photographers</span></div></Link>)}</div></main>}
