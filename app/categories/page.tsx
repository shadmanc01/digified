import Link from 'next/link';import {categories} from '@/lib/data';export default function Categories(){return <main className="shell"><div className="pageHead"><div className="eyebrow">Browse</div><h1>Photography categories</h1></div><div className="grid3">{categories.map(c=><Link className="cameraCard" href={`/category/${c.toLowerCase().replaceAll(' ','-')}`} key={c}><div><div className="eyebrow">Category</div><h3>{c}</h3></div><span className="muted">Explore public photos →</span></Link>)}</div></main>}
