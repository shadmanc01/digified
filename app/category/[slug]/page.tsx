import PhotoGrid from '@/components/PhotoGrid';import {photos} from '@/lib/data';export default async function Category({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const name=decodeURIComponent(slug).replaceAll('-',' ').replace(/\b\w/g,m=>m.toUpperCase());const items=photos.filter(p=>p.category.toLowerCase()===name.toLowerCase());return <main className="shell"><div className="pageHead"><div className="eyebrow">Photography category</div><h1>{name}</h1><p className="muted">Public {name.toLowerCase()} photography from the digified community.</p></div><div className="filterbar"><button className="pill">Popular</button><button className="pill">Latest</button><button className="pill">Camera</button><button className="pill">Lens</button></div><PhotoGrid items={items.length?items:photos}/></main>}
