import PhotoGrid from '@/components/PhotoGrid';import {photos} from '@/lib/data';import {createServerSupabaseClient} from '@/lib/supabase/server';import {getPosts} from '@/lib/photos';export const dynamic='force-dynamic';export default async function Category({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const name=decodeURIComponent(slug).replaceAll('-',' ').replace(/\b\w/g,m=>m.toUpperCase());let items=photos.filter(p=>p.category.toLowerCase()===name.toLowerCase());try{const live=await getPosts(await createServerSupabaseClient(),{categorySlug:slug,limit:100});if(live.length)items=live;}catch{}return <main className="shell"><div className="pageHead"><div className="eyebrow">Photography category</div><h1>{name}</h1><p className="muted">Public {name.toLowerCase()} photography from the digified community.</p></div>{items.length?<PhotoGrid items={items}/>:<div className="empty">No public photos in this category yet.</div>}</main>}
