import PhotoGrid from '@/components/PhotoGrid';import {photos,categories} from '@/lib/data';export default function Explore(){return <main className="shell"><div className="pageHead"><div className="eyebrow">Discover</div><h1>Explore photography</h1><p className="muted">Browse by style, camera, lens or recipe—not just popularity.</p></div><div className="filterbar">{categories.slice(0,10).map(c=><button className="pill" key={c}>{c}</button>)}</div><PhotoGrid items={[...photos].sort((a,b)=>b.saves-a.saves)}/></main>}
