import PhotoGrid from '@/components/PhotoGrid';import {photos} from '@/lib/data';export default function Following(){return <main className="shell"><div className="pageHead"><div className="eyebrow">Your feed</div><h1>Following</h1><p className="muted">A primarily chronological feed from photographers you follow.</p></div><div className="notice" style={{marginBottom:20}}>Demo state: showing sample followed photographers. In production this route is authenticated.</div><PhotoGrid items={photos.slice(0,8)}/></main>}
