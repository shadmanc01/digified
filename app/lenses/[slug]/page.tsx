import PhotoGrid from '@/components/PhotoGrid';import {photos} from '@/lib/data';export default async function LensPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const found=photos.find(p=>p.lens.slug===slug);const name=found?.lens.name||slug.replaceAll('-',' ');const items=photos.filter(p=>p.lens.slug===slug);return <main className="shell"><div className="pageHead"><div className="eyebrow">Lens</div><h1>{name}</h1><p className="muted">Public photographs made with this lens, across camera bodies and categories.</p></div><PhotoGrid items={items.length?items:photos.slice(0,6)}/></main>}
