import {redirect} from "next/navigation";import {createServerSupabaseClient} from "@/lib/supabase/server";import NotificationList from "@/components/NotificationList";
export const dynamic="force-dynamic";
export default async function Notifications(){const supabase=await createServerSupabaseClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect('/login?message=Sign%20in%20to%20view%20activity.');const {data}=await supabase.from('notifications').select('id,type,entity_id,read_at,created_at,profiles!notifications_actor_id_fkey(username,display_name)').eq('user_id',user.id).order('created_at',{ascending:false}).limit(100);return <main className="shell narrow"><div className="pageHead"><div className="eyebrow">Activity</div><h1>Notifications</h1></div><NotificationList items={(data||[]) as any}/></main>}
