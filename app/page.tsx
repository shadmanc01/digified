import Link from 'next/link';import PhotoGrid from '@/components/PhotoGrid';import {photos,cameraStats} from '@/lib/data';import {createServerSupabaseClient} from '@/lib/supabase/server';import {getPosts} from '@/lib/photos';
export const dynamic='force-dynamic';
export default async function Home(){let live=photos;try{const supabase=await createServerSupabaseClient();const rows=await getPosts(supabase,{limit:24});if(rows.length)live=rows;}catch{}return <main className="shell">
  <section className="hero"><div><div className="eyebrow">A home for digital photography</div><h1>Photography, through the cameras that create it.</h1><p>Discover photos, photographers, cameras, lenses and recipes from digital-camera users around the world.</p><form className="heroSearch" action="/search"><span>⌕</span><input name="q" placeholder="Search a camera, lens, photographer or style"/><button className="primary">Search</button></form></div><div className="heroCard"><strong>More than a photo feed.</strong><div>Every image can connect you to the camera, lens, settings and recipe behind it—so discovery can turn directly into inspiration.</div></div></section>
  <section className="section"><div className="sectionHead"><div><div className="eyebrow">Community picks</div><h2>Explore what people are making</h2></div><Link href="/explore">View all →</Link></div><PhotoGrid items={live}/></section>
  <section className="section"><div className="sectionHead"><div><div className="eyebrow">Gear discovery</div><h2>Popular cameras on digified</h2></div><Link href="/cameras">Browse cameras →</Link></div><div className="grid3">{cameraStats.slice(0,6).map(c=><Link className="cameraCard" href={`/cameras/${c.slug}`} key={c.slug}><div><div className="eyebrow">{c.maker}</div><h3>{c.name}</h3></div><div className="cameraMeta"><span>{c.photos} photos</span><span>{c.users} photographers</span></div></Link>)}</div></section>
</main>}
