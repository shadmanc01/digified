import PhotoGrid from '@/components/PhotoGrid';import {photos} from '@/lib/data';export default function Saved(){return <main className="shell"><div className="pageHead"><div className="eyebrow">Private by default</div><h1>Saved</h1><p className="muted">Organize inspiration into collections and save recipes separately.</p></div><div className="filterbar"><button className="pill">All saves</button><button className="pill">Street inspiration</button><button className="pill">Fujifilm recipes</button><button className="pill">Travel ideas</button></div><PhotoGrid items={photos.slice(6,12)}/></main>}
