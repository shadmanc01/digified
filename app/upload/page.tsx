import { redirect } from "next/navigation";import UploadForm from "@/components/UploadForm";import {createServerSupabaseClient} from "@/lib/supabase/server";
export const dynamic="force-dynamic";
export default async function Upload(){const supabase=await createServerSupabaseClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect("/login?message=Sign%20in%20to%20share%20a%20photo.");return <main className="shell"><div className="pageHead"><div className="eyebrow">Create</div><h1>Share a photograph</h1><p className="muted">Publish a single image or carousel with searchable camera, lens, settings, category and recipe data.</p></div><UploadForm/></main>}
