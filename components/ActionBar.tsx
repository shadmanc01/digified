"use client";import {useState} from 'react';
export default function ActionBar({likes,saves}:{likes:number;saves:number}){const [liked,setLiked]=useState(false);const [saved,setSaved]=useState(false);const [reposted,setReposted]=useState(false);return <div className="actionRow"><button onClick={()=>setLiked(!liked)}>{liked?'♥':'♡'} {liked?likes+1:likes}</button><button onClick={()=>setSaved(!saved)}>{saved?'Saved':'Save'} · {saved?saves+1:saves}</button><button onClick={()=>setReposted(!reposted)}>{reposted?'Reposted':'Repost'}</button><button onClick={()=>navigator.clipboard?.writeText(location.href)}>Share</button></div>}
