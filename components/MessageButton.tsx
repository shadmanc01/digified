"use client";import {useRouter} from "next/navigation";import {createBrowserSupabaseClient} from "@/lib/supabase/client";
export default function MessageButton({profileId}:{profileId:string}){const router=useRouter();async function start(){const supabase=createBrowserSupabaseClient();const {data:{user}}=await supabase.auth.getUser();if(!user){router.push("/login?message=Sign%20in%20to%20send%20a%20message.");return;}const {data,error}=await supabase.rpc("start_conversation",{other_user:profileId});if(!error&&data)router.push(`/messages/${data}`);}return <button className="secondary" onClick={start}>Message</button>}
