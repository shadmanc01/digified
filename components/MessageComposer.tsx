"use client";import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";import {createBrowserSupabaseClient} from "@/lib/supabase/client";
export default function MessageComposer({conversationId}:{conversationId:string}){const router=useRouter();const [busy,setBusy]=useState(false);async function send(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const form=e.currentTarget;const body=String(new FormData(form).get('body')||'').trim();const supabase=createBrowserSupabaseClient();const {data:{user}}=await supabase.auth.getUser();if(user&&body){await supabase.from('messages').insert({conversation_id:conversationId,sender_id:user.id,body});await supabase.from('conversations').update({updated_at:new Date().toISOString()}).eq('id',conversationId);form.reset();router.refresh();}setBusy(false);}return <form className="messageComposer" onSubmit={send}><input name="body" required maxLength={4000} placeholder="Write a message…"/><button className="primary" disabled={busy}>{busy?'Sending…':'Send'}</button></form>}
