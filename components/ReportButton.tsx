"use client";import {createBrowserSupabaseClient} from "@/lib/supabase/client";
export default function ReportButton({targetType,targetId}:{targetType:"post"|"comment"|"user";targetId:string}){async function report(){const reason=prompt("Why are you reporting this?");if(!reason)return;const supabase=createBrowserSupabaseClient();const {data:{user}}=await supabase.auth.getUser();if(!user){location.href="/login";return;}const {error}=await supabase.from("reports").insert({reporter_id:user.id,target_type:targetType,target_id:targetId,reason});alert(error?error.message:"Report submitted. Thank you.");}return <button className="textButton" onClick={report}>Report</button>}
